@model IEnumerable<Garage2._0.Models.Vehicle>

@{
    ViewBag.Title = "Index";
   
}

<h3>Garaget
</h3>
@*<table>
    <tr>
        <td>
            <button onclick="location.href='@Url.Action("Create", "Vehicles")'"> Parkera fordon </button>
        </td>
        <td width="500px">
            <form method="get">
                <input type="search" name="searchTerm" class="searchform" value="@ViewBag.SearchExpression" style="display:inline" />
                <input type="submit" value="Sök på reg-nummer" style="display:inline" />
            </form>
        </td>

    </tr>

</table>*@

<div id="block_container">
    <div id="block1">
        <button onclick="location.href='@Url.Action("Create", "Vehicles")'"> Parkera fordon </button>
    </div>

    <div id="block2">
        <form method="get">
            <input type="search" name="searchTerm" class="searchform" value="@ViewBag.SearchExpression" style="display:inline" />
            <input type="submit" value="Sök på reg-nummer" style="display:inline" />
        </form>
    </div>
</div>



<br />

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VehicleType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RegNumber)
        </th>
        
        <th>
            @Html.DisplayNameFor(model => model.Brand)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Make)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VehicleType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RegNumber)
        </td>
        
        <td>
            @Html.DisplayFor(modelItem => item.Brand)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Make)
        </td>
        <td>
            @Html.ActionLink("Ändra", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Mer info", "Details", new { id=item.Id }) |
            @Html.ActionLink("Checka ut", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>

